# 요구사항

- Doctor Plant의 기능 요구사항·명세를 모아둔다.
- 하위에 있는 메모는 요구사항 문서들 중 아직 미확정 된 부분, 고려해야할 부분들을 작성하여 업데이트 하며, 보고시 참고하도록 한다.

## 파일 규칙

- 파일명은 영어, 번호 접두사로 관리한다.
- 문서 제목은 영문, 내용은 한글로 쓴다.

## 메모

> 2026-09-20 `0.set/requirement.md` 구현(타이틀·설정·일시정지·입력) 중 확정이 필요한 항목.

- **일시정지의 "나가기" 대상이 불명확.** 지금은 *게임 종료*(확인 창)로 구현했다.
  타이틀로 돌아가는 경로가 요구사항에 없어 만들지 않았다. 타이틀 복귀 버튼이 필요한지 결정 필요.
- **"로딩" 항목이 비어 있음.** 씬 전환용 로딩 화면(페이드 + 진행률 바)만 만들어 두었다.
  로딩 중 보여줄 연출(일러스트·팁·로고) 미정.
- **저장소 슬롯에 표시할 정보 미정.** 로그 기능이 미구현이라 임시로 `챕터 / 플레이 시간 / 저장 일시`를 표시한다.
  덮어쓰기·삭제·자동 저장 UI는 넣지 않았다.
- **텍스트 크기 적용 범위.** 메뉴 레이아웃이 흔들리는 것을 피하려고 UI 전체가 아니라 *대사 텍스트*에만 적용했다(작게 18 / 보통 22 / 크게 28px).
- **텍스트 속도 범위.** 5~120자/초, 기본 40, 최대치는 "즉시"로 처리. 수치 확정 필요.
- **지원 언어.** 한국어·영어 2종을 코드 내 문자열 테이블로 관리 중이며, 영어 문구는 임시 번역이다.
- **해상도 = 창 크기.** 렌더 해상도(1920x1080)와 화면 비율(16:9)은 고정이라
  이 설정은 창을 얼마나 크게 띄울지만 바꾼다. 어떤 값을 골라도 보이는 범위는 같다.
  목록(1280x720 / 1600x900 / 1920x1080 / 2560x1440)에서 지금 모니터보다 큰 값은 빠지고,
  전체화면에서는 선택이 비활성화된다. 라벨도 "창 크기"로 바꿨다.
  비율이 다른 해상도(21:9 등)를 지원할지는 미정.
- **게임패드 매핑.** A=진행, B=취소, START=일시정지, 방향키·왼쪽 스틱=이동으로 임시 지정.
- **오디오 에셋 없음.** BGM이 없어 코드로 만든 임시 톤을 쓴다(`assets/audio/bgm/` 에 파일이 들어오면 자동 교체).
  효과음은 `door_1`(문 열림) · `door_2`(문 닫힘) · `step_1`(발소리 루프)만 실제 파일을 쓰고,
  노크 · 알림음 · 창문 여는 소리 · 웅성거림은 코드로 만든 임시음이다.
- **한글 폰트 에셋 없음.** 현재는 OS 폰트(맑은 고딕 등)를 사용한다. Steam 배포 전에 재배포 가능한 한글 폰트를 넣어야 한다.

> 2026-09-20 `ch1/03. story progress(1ep).md` 구현(1화 본편) 중 확정이 필요한 항목.

- **미니게임 1의 규칙이 비어 있음** (`ch1/02. mini game.md`). 스토리가 끊기지 않도록
  *환자의 세 부위를 차례로 살피면 끝나는* 임시 진찰 절차(`scenes/ch1/minigame_diagnosis.tscn`)로 대체했다.
  화면에도 "기획 미정" 문구를 띄워 둔다. 규칙이 정해지면 이 씬만 교체하면 된다.
- **장면 2 환자1 대사의 화자 표기가 어긋나 보인다.** 문서에는 `-환자1: 아, 어느쪽이 더 아픈지요? / …음, 머리 옆쪽이면서…`
  로 되어 있는데, 앞 문장은 의사(플레이어)의 질문으로 읽힌다. 지금은 **문서 그대로 셋 다 환자1 대사**로 넣었다.
  의사 대사로 분리할지 확정 필요.
- **컴퓨터 하위 항목의 내용 미정.** `TODAY 환자 리스트 / 원내 메신저 / 인터넷` 세 항목을 선택지로 만들었지만
  각 항목의 실제 내용이 없어 "(세부 내용 미정)"이 붙은 한 줄 설명만 보여준다.
- **"물건을 선택할 수 있음"의 조작 방식.** 진료실 오브젝트(컴퓨터·액자A·액자B·스케줄러·문)를
  *필드에서 다가가 조사하는* 방식으로 구현했다(마우스 클릭도 된다). 정지 화면에서 목록으로 고르는 방식이라면 변경 필요.
- **처방 선택의 결과가 미정.** `처방한다 / 처방하지 않는다`는 `ep1_prescribed_p1` · `ep1_prescribed_p2`
  플래그로 저장만 하고, 대사나 이후 전개는 갈라지지 않는다(문서에도 분기가 없다).
- **배경 에셋 없음.** 병원 로비 · 진료실 · 창밖(해바라기) 모두 단순 도형으로 만들었다(CLAUDE.md 기본 규칙 5).
  `bg_1.png` 는 실외 도시 탑다운 맵이라 병원 내부로 쓰지 않고, 스프라이트 확인용 test 씬 배경으로만 썼다.
- **캐릭터 에셋 미확정.** `doctor_tmp.png` 는 8열 5행(한 칸 64x96) 시트다.
  시트를 뜯어본 결과 **0행 = 제자리(8프레임짜리 미세한 동작), 1행 = 아래 걷기, 2행 = 위 걷기,
  3행 = 왼쪽, 4행 = 오른쪽**으로 보인다(1·2행은 4프레임 걷기를 두 번 반복하는 구성).
  0행과 1행이 둘 다 정면이라 어느 쪽이 제자리인지는 추정이다.
  요청에 따라 **본편 플레이어도 이 임시 시트를 쓴다**(`scenes/field/field_player.tscn`).
  정식 에셋이 들어오면 그 씬의 `sprite_sheet` 만 바꾸면 된다.
  `scenes/test/test_doctor_sprite.tscn` 에서 `1~5` / `Shift+1~5` 로 걷기·제자리 행을 바꿔가며 확정할 수 있다.
  확정되면 `FieldPlayer` 의 `direction_rows` · `idle_rows` 기본값을 맞추고 본편 플레이어에 시트를 지정하면 된다.
- **장·에피소드 타이틀 카드는 요구사항에 없는 연출.** 장면 1 앞과 1화 끝에 카드
  (`CHAPTER 1 / 식물들의 병원 / EP. 1`, `1화 끝`)를 넣었다. 불필요하면 뺀다.
- **1화가 끝나면 test 맵으로 이어진다.** `scenes/test/test_doctor_sprite.tscn`(도시 탑다운 맵)으로 넘어간다.
  2화가 없어 임시로 정한 동작이고, 목적지는 `ep1.tscn` 의 `next_scene` 으로 바꿀 수 있다.
- **`ch1/01.production.md` 가 비어 있음.** 제작 관련 지침이 없어 참고하지 못했다.
